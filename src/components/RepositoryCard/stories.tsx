import { Story, Meta } from '@storybook/react'
import RepositoryCard from '.'

export default {
  title: 'RepositoryCard',
  component: RepositoryCard
} as Meta

export const Default: Story = () => <RepositoryCard />
